apiVersion: v1
kind: Pod
metadata:
  name: invalid-resources-pod
spec:
  containers:
    - name: app
      image: nginx
      resources:
        limits:
          cpu: "200m"
          memory: "128Mi"
        requests:
          cpu: "500m"     # ❌ Request > Limit
          memory: "256Mi" # ❌ Request > Limit
