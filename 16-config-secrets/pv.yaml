apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi   # Storage size
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"   # Local path in node 

      
