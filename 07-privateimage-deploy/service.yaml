apiVersion: v1
kind: Service
metadata:
  name: private-app-svc
  namespace: private-app
  labels:
    app: private-app
spec:
  type: LoadBalancer
  selector:
    app: private-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
