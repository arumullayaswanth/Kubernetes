apiVersion: v1
kind: Service
metadata:
  name: paytam-service
  namespace: prod
spec:
  type: LoadBalancer
  selector:
    app: paytam
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
